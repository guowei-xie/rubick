# 拉比克(Rubick)2.0 产品需求文档 (PRD)

## 一、背景与目标

拉比克是一款面向**不懂 SQL 的业务人员**的自助取数平台:商分部门预先写好带变量的 SQL,业务人员只需填参数、在线运行、下载结果,无需懂代码。

**问题**:公司对数据安全要求提升,而旧版拉比克没有任何账号绑定与权限管控,被迫停用。

**本次升级目标**:在全公司已统一使用飞书(权限体系清晰)的背景下重新上线,核心是补齐**身份、权限、审计**三大安全能力,同时保留"业务人员自助取数"的产品价值。

**核心工作流(本次修正的关键)**:

```
业务用户提数据需求
      │
      ▼
商分同学在产品内编写 SQL、设定变量字段
      │
      ▼
测试运行 → 数据验收通过
      │
      ▼
发布进产品(注册为可运行的取数模板)
      │
      ▼
被授权的业务用户自助填参数运行、按自己的节奏反复取数、下载结果
```

> 当前实现里这条链路是:**开发者/管理员在任务编辑器写 SQL → 测试运行自检 → 上线 → 给指定业务用户授权 →
> 业务用户填参运行、预览、下载**。中间没有独立的「数据验收」卡点(见下文「实现现状」)。

**关键决策(已与用户对齐)**:
1. **SQL 由商分部门在产品内编写与发布**,不是业务用户上传;业务用户只运行已发布模板。**Git 仅用于拉比克项目自身的代码管理,不是产品功能,不参与 SQL 管理。**
2. **数据源**:**直连**公司 **Hive** 与 **MySQL** 两个数据库(Hive 走 HiveServer2/JDBC,MySQL 走标准驱动),只读接入,拉比克自管连接池与只读账号,不经中间网关。把 SQL 下发执行再取回结果。数据源层做成**可插拔连接器**(先实现 Hive + MySQL,后续可扩展 StarRocks/Doris 等)。每个 SQL 模板绑定其**目标数据源与方言**(Hive SQL / MySQL)。
3. **权限**:飞书部门/群组 + RBAC 角色 + 个人精确授权 三种主体统一授权;支持字段级/行级脱敏。
4. **技术栈**:后端 FastAPI(Python),前端 React,平台元数据库 MySQL。
   - 说明:注意区分**两类 MySQL**——① 平台**自身元数据库**(存用户、SQL 模板、权限、任务、审计);② 作为**取数目标**之一的业务 MySQL(与 Hive 并列的外部只读数据源)。二者是不同的库,物理隔离。

---

## 实现现状(重要:本 PRD 为目标蓝图,与当前实现有差异)

> 本 PRD 描述的是完整目标形态;当前代码是精简可用版。下文凡与本段冲突处,**以本段为准**(另见 README)。

**术语对照(产品 UI ↔ 本 PRD / 代码)**

| 产品 UI | PRD / 代码 |
|---------|-----------|
| 任务、任务列表 | SQL 模板 `sql_templates` / `SqlTemplate` |
| 上线 / 重新上线 | 发布 `publish` |
| 下线(进回收站) | 归档 `archive`,`status=archived` |
| 运行记录 | 取数任务 `query_jobs` / `QueryJob` |

**已实现**:飞书 OAuth 登录;在产品内编写 SQL + 变量识别 + SQL 预览 + 测试运行 + 上线/下线 + 版本历史;
业务用户填参 → 异步运行 → 结果预览 → 下载 CSV;按**个人用户**授权(view/run/download);
Hive / MySQL 连接器与按任务的查询超时;审计留痕与**管理端检索 + CSV 导出**;飞书机器人 + 站内通知;结果保留期与自动清理。

**尚未实现 / 已简化**(阅读下文请以此为准):

> - **异步与存储**:未用 Celery+Redis 与 MinIO/S3;实为**独立 DB 轮询 worker** + **本地文件系统** + 带签名 token 的下载端点(§三架构图中的 Celery/Redis、对象存储签名 URL 属目标态)。
> - **组织同步**:**未实现**定时/webhook 通讯录同步,也没有「离职转岗自动回收权限」。用户是**登录时按需建号**(JIT);授权选人时实时搜飞书通讯录,被选中者在**真正授权那一刻**才落库。管理端「用户管理」只列登录过的人(§四.1 的组织同步为目标态)。
> - **权限主体**:只支持**个人用户**。**部门授权已下线**(迁移会清除存量部门授权行并删除 `departments` 表);用户组、RBAC 角色授权未实现(§四.7、§五 为目标态)。
> - **平台角色**:实际是三种固定角色 —— `user` 普通用户(填参取数)、`developer` 开发者(建/改/上线任意任务,不含治理)、`admin` 管理员(开发者能力 + 用户角色赋权/数据源/审计)。**没有独立的「商分」角色**,其职责并入管理员/开发者;RBAC 自定义角色未实现(§二为目标态)。
> - **生命周期**:实际只有 `草稿 → 已上线 →(下线进回收站,可重新上线)`,**没有「测试 / 待验收」状态,也没有验收卡点**;版本表的 `accepted_by/accepted_note` 退化为「上线留痕」(§四.3 的验收流程为目标态)。
> - **参数类型**:实际只有两种形态 —— **单值** 与 **值列表**(由 SQL 写法判定:`字段 IN (:x)` / `NOT IN (:x)` → 值列表),值形态只分 **文本 / 数值**,且**一律必填、无默认值**。日期选择器、枚举单选、部门/用户联动等控件**未实现**;候选值可由作者配「枚举值获取 SQL」提供,值列表还可开启「上传/粘贴」批量输入(§四.3 的参数类型清单为目标态)。
> - **数据脱敏**(字段级/行级)、**结果水印**:均**未实现**(§四.7/§四.8、§九.6 为目标态)。
> - **需求单模块**(requirements)、`masking_rules`/`user_groups`/`roles` 等表:**未建**(§二的「提需求」与 §四.2 为目标态,当前线下提需求)。
> - **定时取数**:未实现(§四.5 为目标态)。
> - **审计**:已落 `audit_logs`/`download_events`,管理端可多维检索并导出 CSV(导出动作本身也留痕),但**非 DB 层强制 append-only**(无触发器/哈希链),也无异常告警(§四.9 的「不可篡改」「异常告警」为目标态)。
> - **只读兜底**:执行前有 SQL 安全网关(sqlglot 解析,仅允许单条 SELECT/WITH);数据库层**只读账号需部署时自行保证**,平台未强制校验。

---

## 二、用户与角色

| 角色 | 说明 | 核心能力 |
|------|------|----------|
| 业务用户 | 不懂 SQL,自助取数 | 提数据需求;浏览有权限的 SQL 模板 → 填参数 → 运行 → 下载结果;按自己节奏反复取数 |
| 商分人员(SQL 作者) | 编写并维护取数 SQL | 接需求 → 在产品内写 SQL、设变量 → 测试运行 → 提交验收 → 发布模板 |
| 管理员 | 数据安全/平台负责人 | 权限授予、数据源配置、脱敏规则、审计查询、用户与角色管理 |

> 注 1:业务用户与商分人员都通过飞书登录,区别在于被授予的角色与权限不同。
> 注 3(当前实现):平台只有三种固定角色 —— **普通用户 / 开发者 / 管理员**,没有「商分」这个角色;
> 上表的「商分人员」职责由**开发者**承担,「管理员」额外独揽 用户角色赋权 / 数据源 / 审计 三块治理。
> 注 2:**SQL 全程只在产品内编写、运行、保存(含版本历史,存于平台 MySQL 元数据库)——SQL 绝不进入 Git。Git 仅托管拉比克项目自身的代码。**

---

## 三、总体架构

```
飞书 OAuth ──► [FastAPI 后端] ──► MySQL(平台元数据)
                    │                  用户/SQL模板/权限/任务/审计
                    │
                    ├─► SQL 编写与发布(商分在产品内完成:草稿→测试→验收→发布)
                    ├─► 任务队列(Celery + Redis)── 异步执行取数        ← 目标态
                    │        │                      实际:独立 DB 轮询 worker(app.worker)
                    │        └─► 只读连接器 ──► Hive / MySQL 数据库(通过 API/驱动下发 SQL,可插拔)
                    │
                    └─► 对象存储(MinIO/S3)── 结果文件 + 签名下载链接    ← 目标态
                             实际:本地文件系统 + 带签名 token 的下载端点
[React 前端] ◄──────┘
飞书机器人 ◄── 任务完成/失败通知

(Git 在图外:仅用于拉比克代码本身的版本控制,与产品运行无关)
```

---

## 四、核心功能模块

### 1. 飞书身份认证与组织同步
- **登录**:飞书扫码 OAuth2.0 登录(网页应用),换取 `open_id`/`union_id`,建立平台会话(JWT + 刷新)。
- **组织同步**:定时(或 webhook)同步飞书通讯录——用户、部门、用户组,存入元数据库。用户离职/转岗自动反映到权限。
  - **当前实现**:无同步任务。用户在**首次飞书登录时按需建号**;授权选人时用操作人的 user_access_token 实时搜通讯录,被选中者在**授权那一刻**才落库。部门/用户组不入库,权限不随组织变动自动回收。
- **未授权拦截**:未登录一律拒绝访问;登录后无任何资源权限则展示空列表。

### 2. 数据需求管理(业务用户 ↔ 商分)
- 业务用户在产品内提交**取数需求单**:描述、期望字段、口径说明、期望时效。
- 商分人员在**需求列表**认领、处理,并把需求与后续产出的 SQL 模板关联。
- 需求状态流转:`待认领 → 处理中 → 已交付(已发布模板)`。
- (此模块让"提需求 → 交付模板"闭环可追溯;若初期想轻量化,可 Phase 2 再做,MVP 先线下提需求。)
- **当前实现:整个模块未做**,需求走线下(IM/工单),产品内没有需求单入口与状态流转。

### 3. SQL 编写与发布管理(商分在产品内完成)
- **在产品内编写**:商分人员在后台编辑器写 SQL,**选择目标数据源(Hive 或 MySQL)**,标注变量占位符(如 `WHERE dt = :start_date`)。
- **变量/参数定义**:为每个变量设定 `name / type / required / default / label / options`,前端据此为业务用户自动渲染填参表单。
- **支持参数类型**:日期、日期区间、字符串、数字、枚举单选/多选、部门/用户(可联动飞书)。
  - **当前实现**:变量从 SQL 里的 `:变量` 自动识别,只分**单值**与**值列表**(`字段 IN (:x)` / `NOT IN (:x)` → 值列表),值形态只分**文本 / 数值**,**一律必填、无默认值**。作者可为每个变量填「变量说明」与「测试值」(测试值兼作业务填参示例);值列表可另配「枚举值获取 SQL」提供候选、并可开启「上传/粘贴」批量输入。上表其余控件类型未实现。
- **测试运行**:商分可用真实参数在只读数仓试跑,预览结果结构与样例数据,用于自检。
  - **当前实现**:编辑器里「测试运行」用各变量的测试值试跑;关联到已保存任务时会在其运行记录里留一条标记为「试跑」的记录(可预览/导出),但不发通知。另有「SQL 预览」只渲染不执行。
- **生命周期与版本**:`草稿 → 测试 → 待验收 → 已发布 → (可下线/归档)`。
  - 每次发布生成一个**版本**(`template_versions`),记录作者、SQL 原文快照、参数定义、发布时间。
  - **只有"已发布"版本对业务用户可运行**;修改需生成新版本再发布,历史版本可回溯。
  - **当前实现**:状态只有 `草稿 → 已上线 →(下线,进回收站,可重新上线)`,无「测试/待验收」。每次保存生成新版本;**编辑已上线任务时新版本自动接替上线**(不必再点一次上线),草稿/已下线则保持原状态。
- **数据验收**:发布前需经过验收(可由需求方或商分负责人标记"验收通过"),验收记录留痕。
  - **当前实现**:无验收卡点,上线即生效;版本上的 `accepted_by/accepted_note` 仅作上线留痕(记录上线人与备注)。
- **防注入绑定**:一律使用**参数化绑定**(`:param`),不拼接字符串;结构性参数(动态表名、IN 列表)走白名单校验。

### 4. 数据源与执行引擎(只读 + 资源治理)
- **接入方式**:拉比克后端**直连**目标库把 SQL 下发执行、取回结果——
  - **Hive**:直连 HiveServer2(JDBC/Thrift)提交;查询多为批处理、**耗时较长**,强化异步任务的必要性。
  - **MySQL**:标准驱动直连,适合中小结果集的即时查询。
- **只读隔离**:Hive 与 MySQL 均使用**只读账号**(无 DDL/DML 权限),从数据库层面兜底。
- **SQL 安全网关**:执行前解析——仅允许单条 `SELECT`;拒绝多语句、注释穿越、危险关键字;按模板绑定的**方言**(Hive SQL / MySQL)分别校验。
- **资源治理**:查询超时、结果行数上限、结果文件大小上限、每用户并发排队、慢查询强制终止。
  - **当前实现**:已有**查询超时**(可按任务覆盖;默认 MySQL 120s、Hive 3600s,MySQL 用 `MAX_EXECUTION_TIME`、Hive 到点客户端 `cancel`)与**结果行数上限**(`MAX_RESULT_ROWS`,超出标记截断)。**文件大小上限、每用户并发配额未实现**;排队是单 worker 全局 FIFO,不区分用户。结果文件保留 `RESULT_RETENTION_DAYS` 天后由 worker 清理。
- **可插拔连接器**:抽象 `DataSourceConnector` 接口,**先实现 Hive 与 MySQL 两种连接器**,后续可加 StarRocks/Doris/ClickHouse。

### 5. 业务用户自助取数
- **浏览**:业务用户看到自己有权限的已发布模板(按业务域/标签/搜索)。
- **填参运行**:按模板参数定义填表单(带必填/格式校验)→ 提交运行。
- **自主节奏**:同一模板可**随时反复运行**——用户想要最新数据时自行再跑一次;每次运行是独立任务与独立结果。
- **(可选)定时取数**:Phase 2+ 支持"每天/每周定时自动跑并推送",满足周期性取数场景。

### 6. 异步取数任务
- 提交即入队(`queued → running → success/failed`),前端可查看进度、历史任务列表。
- 完成/失败通过**飞书机器人**推送通知(含下载入口)。
  - **当前实现**:飞书卡片 + **站内通知**双写(未配飞书凭证时仅站内),点通知深链到该任务的「运行记录」。
- 结果写入对象存储,生成**带时效的签名下载链接**。
  - **当前实现**:结果写**本地文件系统**(`RESULT_DIR`),下载走后端端点 + 带时效签名 token(`DOWNLOAD_URL_EXPIRE_SECONDS`),非对象存储签名 URL。

### 7. 权限模型(统一授权 + 脱敏)
- **主体(Subject)**:个人用户 / 飞书部门 / 飞书用户组 / RBAC 角色。
  - **当前实现**:只有**个人用户**。部门授权曾上线后又下线(迁移会清除存量部门授权行);用户组/角色未实现。
- **资源(Resource)**:SQL 模板、数据源。
  - **当前实现**:只对**任务(SQL 模板)**授权;数据源不参与授权(管理员统一配置)。
- **动作(Action)**:查看 / 运行 / 下载;商分侧额外有 编写 / 发布。
  - **当前实现**:授权动作就是 `view / run / download` 三个;「编写 / 上线」不走授权表,由平台角色(开发者/管理员)决定,作者对自己建的任务恒有全部权限。
- **判定**:用户有效权限 = 个人授权 ∪ 所属部门/组授权 ∪ 所属角色授权(并集,可后续加"拒绝优先")。
  - **当前实现**:有效权限 = 个人授权 ∪ 作者身份;管理员/开发者对全部任务短路放行。
- **数据脱敏**:
  - **字段级**:对敏感列(手机号、身份证等)按规则掩码,规则可绑定角色/部门。
  - **行级**:按用户属性注入过滤条件(如"仅看本部门数据")。

### 8. 结果下载与数据安全
- 格式:CSV / Excel(可配)。
  - **当前实现**:只有 **CSV**(带 UTF-8 BOM,Excel 可直接打开);下载前先在页面里预览前 50 行确认。
- **水印**:导出文件内嵌"取数人 + 时间 + 任务号",便于泄露溯源。(**未实现**;溯源目前只靠 `download_events` 与审计)
- 下载链接时效 + 大小上限;可选"仅在线预览不落地"模式用于高敏数据。
  - **当前实现**:链接时效已有(签名 token);大小上限与「仅预览不落地」未实现。

### 9. 审计与日志(核心合规能力)
- **记录内容**:谁(飞书身份)、何时、运行了哪个模板(含版本)、参数值、命中数据源、返回行数、生成的结果文件、每次下载事件、IP/设备。
  - **当前实现**:覆盖 登录、提交/运行/失败取数、下载、任务新建/编辑/上线/下线/恢复、任务授权与撤销、平台角色变更、数据源增改删、审计导出;动作码字典的单一真源在 `models/audit.py::ACTION_META`。
- **不可篡改**:审计为追加写(append-only),独立表存储。
  - **当前实现**:append-only 是**应用层约定**(只 INSERT,无更新/删除路径),DB 层未强制(无只写账号/触发器/哈希链)。
- **管理端**:按用户/时间/模板/数据源多维检索与导出;可选异常告警(如短时间大量取数)。
  - **当前实现**:可按 操作人 / 动作 / 资源类型 / 时间范围 检索并按当前筛选导出 CSV(导出本身留痕);异常告警未实现。

### 10. 管理后台
- 用户与组织查看(飞书同步)、角色管理(RBAC)。
- 权限矩阵配置(主体 × 资源 × 动作)。
- 数据源配置与连接测试、脱敏规则配置。
- SQL 模板与版本管理、验收记录。
- 审计查询与导出、任务监控。

> **当前实现的管理端只有三个页面**(仅管理员可见):
> - **用户管理**:列出登录过的用户,给单人切换 普通用户 / 开发者 / 管理员(无 RBAC 自定义角色、无组织树)。
> - **数据源**:增删改查 + 连接测试(密码留空表示不改;仍被任务引用时不允许删除)。无脱敏规则配置。
> - **审计**:多维检索 + CSV 导出。
>
> 任务与授权不在独立管理页,而是在「任务列表」的卡片上就地操作(编辑 / 上线 / 下线 / 授权 / 运行记录);
> 没有独立的权限矩阵页与任务监控大盘。

---

## 五、平台元数据模型(MySQL,关键表)

> 当前实现的表统一带 `rubick_` 前缀(见 `core/database.py::tbl`),可与其它系统共用一个库。
> 下表标注「已建 / 未建」以对齐实现。

- `users`(已建;飞书 JIT 建号)/ `departments`、`user_groups`(**未建**,departments 曾建后由迁移删除)、`roles` / `user_roles`(**未建**,角色是 users.role 上的固定枚举)
- `data_sources`(已建;密码在库内加密存储 `EncryptedText`,尚未接外部密钥管理)
- `requirements`(需求单:提出人、描述、状态、关联模板)(**未建**)
- `sql_templates` / `template_versions`(已建;SQL 快照、参数定义、作者、生命周期状态、上线留痕)
- `permissions`(已建;subject_type 当前恒为 `user`)
- `masking_rules`(字段级/行级规则)(**未建**)
- `query_jobs`(已建;状态、参数、耗时、行数、结果引用、实际执行 SQL、运行来源 run/test)
- `audit_logs`、`download_events`(已建;append-only 为应用层约定)
- `notifications`(已建;站内通知,飞书推送的本地镜像/兜底,PRD 未列)

---

## 六、非功能性需求

- **安全**:数仓只读账号 + SQL 网关 + 参数化绑定;密钥用密钥管理(不落库明文);全 HTTPS。
- **性能**:异步队列削峰;资源上限防拖垮数仓;结果分页/流式导出。
- **可维护**:数据源连接器可插拔;SQL 模板版本化,可回溯可下线。
- **可观测**:任务与审计双日志;关键指标(任务量、失败率、慢查询)监控。

---

## 七、分期上线计划(建议)

- **Phase 1(MVP)**:飞书登录 + 组织同步;商分在产品内写 SQL + 设变量 + 测试运行 + 发布;业务用户填参**同步**运行(带资源上限)+ 下载;个人 + 部门授权;基础审计。目标:安全可用,替代旧版。
- **Phase 2**:需求单闭环;异步任务 + 飞书通知;RBAC 角色;字段/行级脱敏;结果水印。
- **Phase 3**:定时取数;审计异常告警;模板验收流程精细化;高级检索与自助化。

> **实际进度**:Phase 1 已完成(组织同步与部门授权除外——见「实现现状」),并已提前落地 Phase 2 的
> **异步任务 + 飞书/站内通知**;取数从一开始就走异步 worker,不是 Phase 1 设想的同步运行。
> 仍未做:需求单闭环、RBAC 角色、脱敏、水印,以及整个 Phase 3。

---

## 八、待确认的开放项(实现前建议敲定)

1. ~~Hive 直连的**认证方式**~~(已落地:数据源配置里可选 `auth`,LDAP 已跑通;`KERBEROS` 选项虽在下拉里,但所需的 kerberos/gssapi 依赖未安装,**未验证可用**)。
2. ~~数据验收由谁标记"通过"~~(已定:**不设验收卡点**,开发者/管理员自检后直接上线,上线人与备注留痕)。
3. 高敏数据是否需要"仅在线预览、禁止下载"这一硬约束。(未实现;现状是任何有 `download` 授权的人都可下载完整 CSV)
4. ~~飞书应用形态~~(已定:自建应用走 OAuth,登录申请通讯录搜索权限以支持授权选人)。
5. ~~需求单模块 MVP 是否要做~~(已定:**不做**,线下提需求)。

---

## 九、验收方式(如何验证做对了)

MVP 完成后按以下端到端路径验证(标注 ⛔ 的项对应尚未实现的能力,当前不适用):
1. **登录**:飞书扫码登录成功,未登录访问被拒;首次登录自动建号并出现在「用户管理」。
2. **编写上线**:开发者写一条带变量的 SQL → 测试运行看到结果 → 上线 → 生成版本记录。
3. **权限**:给某业务用户授权该任务 → 该用户可见并可运行;未授权任务不可见、直接调用被拒。(⛔ 部门授权)
4. **取数**:业务用户选任务 → 填参数(校验生效)→ 运行 → 预览 → 下载 CSV,结果正确;超时/超行数被限制;同一任务可反复再跑得到最新数据。
5. **只读兜底**:含 DDL/DML 或多语句的 SQL 被安全网关或只读账号拒绝。
6. ⛔ **脱敏**:同一模板,授权掩码的用户看到手机号被掩码,未掩码用户看到明文。
7. **审计**:每次运行与下载都在 `audit_logs`/`download_events` 留痕,含身份、时间、模板版本、参数、行数;管理端可检索导出 CSV。
8. **回收站**:下线任务后业务用户不可再运行,任务进回收站;从回收站重新上线后恢复可运行,两次操作在审计里分别是「任务下线」「任务从回收站恢复」。
